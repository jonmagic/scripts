{
  "name": "jonmagic-scripts",
  "displayName": "Jonmagic Scripts",
  "description": "Utilities for my Brain repo (Daily Projects, wikilinks, etc.)",
  "version": "0.0.0",
  "publisher": "jonmagic",
  "repository": {
    "type": "git",
    "url": "https://github.com/jonmagic/scripts",
    "directory": "packages/vscode"
  },
  "license": "UNLICENSED",
  "engines": {
    "vscode": "^1.90.0"
  },
  "activationEvents": [
    "onLanguage:markdown"
  ],
  "categories": ["Other"],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "jonmagic.scripts.createDailyProjectNote",
        "title": "Jonmagic: Create Daily Project Note"
      },
      {
        "command": "jonmagic.scripts.addFrontmatter",
        "title": "Jonmagic: Add Frontmatter to Current File"
      },
      {
        "command": "jonmagic.brain.previousWeek",
        "title": "Brain: Previous Week",
        "icon": "$(arrow-left)"
      },
      {
        "command": "jonmagic.brain.nextWeek",
        "title": "Brain: Next Week",
        "icon": "$(arrow-right)"
      },
      {
        "command": "jonmagic.brain.goToCurrentWeek",
        "title": "Brain: Go to Current Week",
        "icon": "$(home)"
      },
      {
        "command": "jonmagic.brain.refresh",
        "title": "Brain: Refresh",
        "icon": "$(refresh)"
      }
    ],
    "configuration": {
      "title": "Jonmagic Scripts",
      "properties": {
        "jonmagic.brain.path": {
          "type": "string",
          "default": "~/Brain",
          "description": "Path to the Brain repository folder"
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "brain-sidebar",
          "title": "Brain",
          "icon": "$(book)"
        }
      ]
    },
    "views": {
      "brain-sidebar": [
        {
          "id": "brainWeekView",
          "name": "This Week",
          "contextualTitle": "Brain"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "jonmagic.brain.previousWeek",
          "when": "view == brainWeekView",
          "group": "navigation@1"
        },
        {
          "command": "jonmagic.brain.nextWeek",
          "when": "view == brainWeekView",
          "group": "navigation@2"
        },
        {
          "command": "jonmagic.brain.goToCurrentWeek",
          "when": "view == brainWeekView",
          "group": "navigation@3"
        },
        {
          "command": "jonmagic.brain.refresh",
          "when": "view == brainWeekView",
          "group": "navigation@4"
        }
      ]
    }
  },
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "watch": "node ./scripts/build.mjs --watch",
    "package": "bunx @vscode/vsce package --no-dependencies --skip-license",
    "typecheck": "bun tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@jonmagic/scripts-core": "workspace:*"
  },
  "devDependencies": {
    "@types/vscode": "^1.90.0",
    "@vscode/vsce": "^3.2.2",
    "esbuild": "^0.24.2",
    "typescript": "^5.8.2"
  }
}
